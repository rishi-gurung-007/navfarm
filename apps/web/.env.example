# Browser requests always use the same-origin /api/v1 and /uploads boundaries.
# These values are read only by the Next.js server/build and are not exposed in
# the browser bundle.
NAVFARM_API_MODE=proxy
NAVFARM_API_UPSTREAM_URL=http://127.0.0.1:2877

{
  "name": "web",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "start": {
        "executor": "nx:run-commands",
        "continuous": true,
        "dependsOn": ["build"],
        "options": {
          "cwd": "apps/web",
          "command": "next start --hostname 0.0.0.0 --port 3002"
        }
      },
      "typecheck": {
        "executor": "nx:run-commands",
        "options": {
          "cwd": "apps/web",
          "commands": [
            "next typegen",
            "tsc --noEmit -p tsconfig.json"
          ],
          "parallel": false
        }
      }
    }
  },
  "dependencies": {
    "next": "~16.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-qr-code": "^2.2.0",
    "recharts": "^3.10.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.2",
    "tailwindcss": "^4.3.2"
  }
}

# NAVFarm does not use a shared root PORT variable. A shared PORT leaks between
# the API and Next.js processes and can make both try to listen on the API port.
#
# Configure the API in apps/api/.env and the web proxy in apps/web/.env.local.
# Redis is not used by the current application.
